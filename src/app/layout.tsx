import type { Metadata } from "next";

import classNames from "classnames";

import { Inter } from "next/font/google";
const inter = Inter({ subsets: ["latin"] });

import "./globals.css";

import {
  Header,
  Container,
  Footer,
  PageHeader,
  ProjectMainMessage,
  ThemeSwither,
} from "@/components";

export const metadata: Metadata = {
  title: "ReactifyWP - Your Wordpress Theme",
  description: "Generated by create next app and smart hands",
};

const RootLayout = ({ children }: { children: React.ReactNode }) => {
  const siteUnderMaintance: Boolean = false;

  return (
    <html lang="en">
      <body
        className={classNames(
          inter.className,
          "flex-cstart flex-dcol mx-auto max-w-wrapper-page-sz overflow-x-hidden p-2xl tablet:p-md",
        )}
      >
        <Header />
        <main className="flex-cstart flex-dcol min-h-screen w-full">
          <Container width="lg">
            <PageHeader
              title="Do you like this Stories ?"
              text="Take a moment to explore our collection of posts and discover the
          simplicity within."
            />
            {children}
          </Container>
        </main>
        <Footer />
        {siteUnderMaintance && <ProjectMainMessage />}
        <ThemeSwither />
      </body>
    </html>
  );
};

export default RootLayout;
