import { FC } from "react";

import TagsPageAll from "../tags/page";

interface ITagPageProps {}

const TagPageAll: FC<ITagPageProps> = (props) => {
  return <TagsPageAll />;
};

export default TagPageAll;
