import { FC } from "react";

import TagsPageAll from "../tags/index";

interface TagPageProps {}

const TagPageAll: FC<TagPageProps> = (props) => {
  return <TagsPageAll />;
};

export default TagPageAll;
